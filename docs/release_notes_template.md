# Release Notes Template

## Highlights
- TODO: bullets for the tag (features, fixes, IR changes, integrations).

## VS Code Sample
- CI uploads VSIX artifact: `neurocode-vscode-sample-vsix-<tag>.vsix` (from `examples/vscode-neurocode`).
- Build pipeline: `.github/workflows/release.yml` (tags) and `vscode-sample.yml` (pushes touching sample/docs).

## CLI / API
- TODO: summarize CLI/API changes.

## IR / Embeddings
- TODO: summarize IR schema or embedding changes.

## Breaking Changes
- TODO: list breaking changes (if any).

## Upgrade Notes
- TODO: how to upgrade (pip install, rebuild IR, etc.).

## Thanks
- TODO: acknowledgments.
